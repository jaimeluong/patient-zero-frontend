const defaultStyle = {
    display: "flex",
    justifyContent: "center",
    marginTop: "300px"
};

const Home = () => {
    return(
        <div className="Home" style={defaultStyle}>
            <h1>Home content</h1>
        </div>
    );
}

export default Home;
