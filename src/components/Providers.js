const defaultStyle = {
    display: "flex",
    justifyContent: "center",
    marginTop: "300px"
};

const Providers = () => {
    return(
        <div className="Providers" style={defaultStyle}>
            <h1>Providers content</h1>
        </div>
    );
}

export default Providers;
