const defaultStyle = {
    display: "flex",
    justifyContent: "center",
    marginTop: "300px"
};

const Appointments = () => {
    return(
        <div className="Appointments" style={defaultStyle}>
            <h1>Appointments content</h1>
        </div>
    );
}

export default Appointments;
